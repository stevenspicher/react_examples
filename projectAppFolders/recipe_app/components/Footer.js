const Footer = ({ info }) => {
    
    return (
        <nav className="footer">
            <h1>
                <a href={info}>Website</a>
            </h1>
        </nav>
    );
}

export default Footer;